import React from 'react';
import Nav from './Nav';
import Player from './Player';

const Mainboard = () => (
  <main className="main">
    <Nav/>
    <Player />
  </main>
);

export default Mainboard;
